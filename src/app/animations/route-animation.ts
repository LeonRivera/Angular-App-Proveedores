import {
    trigger,
    transition,
    style,
    query,
    group,
    animateChild,
    animate, 
    keyframes,
} from '@angular/animations'

// //basic
//  export const fader = 
//  trigger('routeAnimations', [
//     transition('* <=> *')
//  ])